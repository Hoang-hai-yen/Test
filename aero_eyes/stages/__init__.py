"""Pipeline stages. Each is independently runnable as a CLI module:

    python -m aero_eyes.stages.stage1 --config configs/config.yaml --sample <id>
    ...
    python -m aero_eyes.stages.run_all --config configs/config.yaml
"""
